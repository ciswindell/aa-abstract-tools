## Relevant Files

- `abstract_renumber.py` - Current Tk controller; replace legacy usages and slim to entrypoint.
- `app/tk_app.py` - New Tk controller module to host UI wiring (to be created).
- `core/services/renumber.py` - Orchestration service; import cleanup and logic DRY-up.
- `core/services/validate.py` - Central validation orchestration.
- `validators/input_sheet_validator.py` - Single source for Excel header validation.
- `core/transform/excel.py` - Pure Excel transforms; unit test target.
- `core/transform/pdf.py` - Pure PDF transforms; unit test target.
- `adapters/excel_repo.py` - Excel IO adapter; ensure atomic write usage only.
- `adapters/pdf_repo.py` - PDF IO adapter.
- `adapters/logger_tk.py` - Logger adapter for Tk.
- `core/interfaces.py` - Protocols; ensure boundaries remain clean.
- `core/models.py` - Options/Result models used by services.
- `core/config.py` - Centralized configuration (to be created).
- `fileops/files.py` - Atomic write/backup helpers used by adapters.
- `tests/core/transform/test_excel.py` - Unit tests for Excel transforms (to be created).
- `tests/core/transform/test_pdf.py` - Unit tests for PDF transforms (to be created).
- `tests/core/services/test_validate.py` - Unit tests for validation service (to be created).
- `tests/core/services/test_renumber_service.py` - Service-level test with fakes (to be created).
- `pyproject.toml` - Ruff/pytest configuration (to be created/updated).

### Notes

- Use pytest for tests and aim for ≥ 80% coverage on `core/transform/*` and `core/services/validate.py`.
- Keep imports at module top-level throughout the repository.
- Preserve current GUI behavior and strings while moving controller wiring.

## Tasks

- [ ] 1.0 Replace legacy processor usages in `abstract_renumber.py` with services/adapters
  - [x] 1.1 Remove imports of `ExcelProcessor` and `PDFProcessor`.
  - [x] 1.2 Remove attributes and code paths referencing `excel_processor`/`pdf_processor`.
  - [x] 1.3 Instantiate `TkLogger`, `ExcelOpenpyxlRepo`, `PdfPyPDF2Repo`, `ValidationService`, `RenumberService`.
  - [x] 1.4 Build `Options` from GUI state and call `RenumberService.run(...)`.
  - [x] 1.5 Replace any downstream save/rename logic with adapter/service outputs as needed.
  - [x] 1.6 Manual smoke test of the GUI flow (import-level smoke test completed).
- [ ] 2.0 Centralize configuration in `core/config.py` and inject into app/services
  - [x] 2.1 Create `core/config.py` with `DEFAULT_REQUIRED_COLUMNS`, `DEFAULT_SORT_COLUMNS`, `DEFAULT_SHEET_NAME`.
  - [x] 2.2 Update `abstract_renumber.py` to read required columns and defaults from config.
  - [x] 2.3 Update `ValidationService` initialization to use config defaults.
  - [x] 2.4 Ensure transforms use passed parameters or config defaults consistently.
- [ ] 3.0 Consolidate validation to single source and update `ValidationService`
  - [x] 3.1 Ensure `validators/input_sheet_validator.py` is the only header validation source.
  - [x] 3.2 Update `ValidationService` to compose validators and raise on failures.
  - [x] 3.3 Replace any ad-hoc validation/warnings in `abstract_renumber.py` with service calls.
  - [x] 3.4 Add tests for missing headers and duplicate required headers.
- [ ] 4.0 Clean up `RenumberService` (top-level imports, reuse `extract_original_index`, remove unused work)
  - [x] 4.1 Move `natsort` import to module top level.
  - [x] 4.2 Use `core/transform/pdf.extract_original_index` in `_merge_bookmarks_with_titles`.
  - [x] 4.3 Remove unused `build_original_index_mapping` computation if not referenced.
  - [x] 4.4 Ensure error handling logs via `Logger` and returns `Result` consistently.
- [ ] 5.0 Relocate Tk controller to `app/tk_app.py` and slim `abstract_renumber.py`
  - [x] 5.1 Create `app/` directory and add `app/tk_app.py`.
  - [x] 5.2 Move `AbstractRenumberGUI` and `AbstractRenumberTool` into `app/tk_app.py`.
  - [x] 5.3 Keep `abstract_renumber.py` as thin entrypoint that imports and runs Tk app.
  - [x] 5.4 Fix imports and run a smoke test to verify identical behavior.
- [ ] 6.0 Enforce adapter/transform boundaries (no business logic in adapters; atomic write in Excel adapter)
  - [x] 6.1 Review `adapters/excel_repo.py` and keep only IO/formatting responsibilities.
  - [x] 6.2 Ensure `fileops/files.atomic_write_with_template` remains the write path.
  - [x] 6.3 Verify `core/transform/*` remain pure and side-effect free (no IO imports).
  - [x] 6.4 Remove any stray business logic from adapters, if present.
- [ ] 7.0 Add/ensure `natsort` top-level imports and dependency rules across modules
  - [ ] 7.1 Confirm `natsort` in `requirements.txt` and import at module top where used.
  - [x] 7.1 Confirm `natsort` in `requirements.txt` and import at module top where used.
  - [x] 7.2 Repo-wide search to move any function-level imports to top level.
  - [x] 7.3 Run linter to detect and fix import style issues.
- [ ] 8.0 Add tests for transforms and validation; add service orchestration test with fakes
  - [ ] 8.1 `tests/core/transform/test_excel.py`: `clean_types`, `sort_and_renumber` edge cases.
  - [x] 8.1 `tests/core/transform/test_excel.py`: `clean_types`, `sort_and_renumber` edge cases.
  - [ ] 8.2 `tests/core/transform/test_pdf.py`: `extract_original_index`, `make_titles`, `detect_page_ranges`.
  - [x] 8.2 `tests/core/transform/test_pdf.py`: `extract_original_index`, `make_titles`, `detect_page_ranges`.
  - [ ] 8.3 `tests/core/services/test_validate.py`: missing/duplicate headers; bookmark conflicts.
  - [x] 8.3 `tests/core/services/test_validate.py`: missing/duplicate headers; bookmark conflicts.
  - [x] 8.4 `tests/core/services/test_renumber_service.py`: use fake repos/logger; validate orchestration.
  - [ ] 8.5 Add fixtures and minimal fakes to isolate IO.
- [ ] 9.0 Add Ruff (PEP 8) and configure pytest in `pyproject.toml` (skipped)
  - [ ] 9.1 Add `pyproject.toml` with `tool.ruff` and `tool.pytest.ini_options`. (skipped)
  - [ ] 9.2 Run `ruff` and fix reported issues; ensure imports are at top level. (skipped)
  - [ ] 9.3 Verify pytest discovery and run all tests. (skipped)
- [ ] 10.0 Remove dead code and delete `excel_processor.py` and `pdf_processor.py` after verification
  - [x] 10.1 Repo-wide search for unused functions/classes/variables and remove them.
  - [x] 10.2 Confirm there are zero references to legacy processors.
  - [x] 10.3 Delete `excel_processor.py` and `pdf_processor.py`.
  - [ ] 10.4 Final test run and manual GUI smoke test.
